import * as muzidigbig from '../index';
test('says hello', () => {
  expect(muzidigbig.sayHello('World')).toBe('Hello, World!');
  // var arr = [1, 5, 10, 21, 22, 25, 43, 55, 100];
  // expect(muzidigbig.array.randomArr(arr))
  // expect(muzidigbig.browser.getQueryObject('http://www.baidu.com?id = 1022 & name = muzidigbig & age = 18'))
});